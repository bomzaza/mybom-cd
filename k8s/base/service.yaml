apiVersion: v1
kind: Service
metadata:
  name: mybom
  labels:
    app.kubernetes.io/name: mybom
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mybom
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP